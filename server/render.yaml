services:
  - type: web
    name: billforge-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node ./server/index.js
    envVars:
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
