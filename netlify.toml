[build]
  command   = "npx expo export -p web"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirect all routes to index.html for Expo Router SPA
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
